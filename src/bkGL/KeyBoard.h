#pragma once

#ifndef BK_KEYBOARD_H
#define BK_KEYBOARD_H

#include <bk/CopyablePIMPL>
#include <bkGL/lib/bkGL_export.h>
#include <bkGL/EKey.h>

namespace bk
{
  class BKGL_EXPORT KeyBoard
  {
      //====================================================================================================
      //===== MEMBERS
      //====================================================================================================
      class Impl;

      bk::cpimpl<Impl> _pdata;

      //====================================================================================================
      //===== CONSTRUCTORS & DESTRUCTOR
      //====================================================================================================
    public:
      /// @{ -------------------------------------------------- CTOR
      KeyBoard();
      KeyBoard(const KeyBoard&);
      KeyBoard(KeyBoard&&) noexcept;
      /// @}

      /// @{ -------------------------------------------------- DTOR
      ~KeyBoard();
      /// @}

      //====================================================================================================
      //===== GETTER
      //====================================================================================================
      /// @{ -------------------------------------------------- GET BUTTON INFORMATION
      [[nodiscard]] bool key_is_pressed(Key_ k) const;
      /// @}

      //====================================================================================================
      //===== SETTER
      //====================================================================================================
      /// @{ -------------------------------------------------- OPERATOR =
      [[maybe_unused]] KeyBoard& operator=(const KeyBoard&);
      [[maybe_unused]] KeyBoard& operator=(KeyBoard&&) noexcept;
      /// @}

      /// @{ -------------------------------------------------- SET PRESSED
      void set_key_pressed(Key_ k, bool pressed);
      /// @}
  }; // class KeyBoard
} // namespace bk

#endif //BK_KEYBOARD_H
